services:
  - type: web
    name: compass-os
    runtime: static
    buildCommand: echo "Compass AI is a prebuilt static release"
    staticPublishPath: .
    autoDeployTrigger: commit
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
