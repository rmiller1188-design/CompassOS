import { NextRequest, NextResponse } from "next/server";
import { z } from "zod";
import { requireApiUser } from "@/lib/auth";
import { createAdminClient } from "@/lib/supabase/admin";
import { normalizeAppearance, settingsRecord } from "@/lib/personalization";
const inputSchema=z.object({mode:z.enum(["system","light","dark"]),accent:z.enum(["violet","blue","green","orange","rose","graphite","cyan","red","gold","indigo","lavender","mint","teal","brown"]),density:z.enum(["compact","comfortable","spacious"]),radius:z.enum(["square","soft","round","pill"]),surface:z.enum(["solid","glass","paper","midnight","frosted","tinted"]),motion:z.enum(["full","reduced","none"]),scale:z.enum(["small","normal","large"]),navMode:z.enum(["expanded","compact","icons"]),chrome:z.enum(["minimal","balanced","expressive"]),background:z.enum(["calm","gradient","graphite","warm","ocean","lavender","mint","cream","dusk","black"]),workspaceWidth:z.enum(["focused","standard","wide","fluid"]),spacing:z.enum(["tight","standard","airy"]),elevation:z.enum(["flat","soft","raised"]),headerMode:z.enum(["sticky","static","hidden"]),reducedTransparency:z.boolean(),showHelperText:z.boolean(),showTimestamps:z.boolean()});
export async function PUT(request:NextRequest){try{const user=await requireApiUser();const appearance=normalizeAppearance(inputSchema.parse(await request.json()));const admin=createAdminClient();const{data:profile,error}=await admin.from("profiles").select("id,settings").eq("owner_id",user.id).eq("kind","personal").single();if(error||!profile)return NextResponse.json({error:"profile_not_found"},{status:404});const settings={...settingsRecord(profile.settings),appearance};const update=await admin.from("profiles").update({settings,updated_at:new Date().toISOString()}).eq("id",profile.id).eq("owner_id",user.id);if(update.error)return NextResponse.json({error:"appearance_save_failed"},{status:500});return NextResponse.json({appearance});}catch(error){const message=error instanceof Error?error.message:"";if(message==="UNAUTHORIZED")return NextResponse.json({error:"unauthorized"},{status:401});if(error instanceof z.ZodError)return NextResponse.json({error:"invalid_appearance"},{status:400});return NextResponse.json({error:"appearance_save_failed"},{status:500});}}
