# 2026-08-10 — P7AD lease-fenced cursor advancement

- Started from exact P7AC head `dc308fb061aa635fded2c25946f907b00a46b329`.
- Added lease-aware final cursor checkpoint propagation for mail, calendar, and contacts incremental sync runners.
- Added service-role-only `save_sync_cursor_with_account_lease` Supabase RPC. The function locks the matching account lease row, validates account/provider/worker/token ownership, requires an unexpired lease and active connected account, and advances `sync_cursors` in the same transaction.
- Added sanitized retryable `SYNC_CURSOR_FENCE_*` failures for invalid scope, lost ownership, and persistence-backend failures.
- Added deterministic tests for fenced RPC routing, lease loss, backend diagnostic sanitization, scope drift, and all-resource checkpoint propagation.
- Bumped package version to 0.47.0 and updated `BUILD_ROADMAP.md`.
- Implementation candidate `bdea389ec38e863afbfea534dedf1239e5e0a2cf` passed GitHub Actions `Validate production core` run 578. Final documentation head still requires the same exact-head validation before P7AD is reported reviewable.
- Live Supabase migration execution, row-lock contention, lease expiry/reclaim races, service-role authorization, crash recovery, and durable cursor progression remain infrastructure-blocked.
