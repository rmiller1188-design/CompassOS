# 2026-08-10 — P7AB sync lease heartbeat integration

## Build pass

Continued from exact P7AA head `cc0c86adb458ae8a3a22802385cbb40be84b1260` on isolated branch `feat/p7ab-sync-lease-heartbeats`.

Implemented production-side lease renewal through the unified multi-account sync path. The coordinator now requires acquire/heartbeat/release semantics for leased execution, heartbeats before each resource runner, carries forward rotating lease tokens, and releases the latest lease generation. Default mail, calendar, and contacts incremental runners heartbeat after each provider page fetch before normalized data or cursor persistence.

Lease heartbeat backend/loss failures are sanitized, retryable sync-control failures. Once lease ownership is lost or cannot be proven, remaining resources for only that connected account are skipped rather than continuing under stale ownership. The page that observed failed renewal cannot advance its cursor.

Added deterministic coverage for rotating heartbeat tokens, refreshed-token release, heartbeat loss, cursor non-advancement, affected-account resource short-circuiting, and fail-closed lease-manager composition. Package version is `0.45.0`.

## Validation state

Repository CI validation is required on the exact final branch head before P7AB can be marked reviewable. Live Supabase RPC/RLS behavior, true long-running provider requests, multi-worker contention, scheduler crash/restart, and durable cursor progression remain infrastructure-blocked in this build context.
