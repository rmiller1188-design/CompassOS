# 2026-08-10 — P7AC

Built the in-flight sync lease guard on top of P7AB. Long-running mail/calendar/contacts provider page reads now heartbeat while pending, abort through `AbortSignal` when lease ownership is lost, and retain the existing post-fetch heartbeat before any normalized persistence or cursor advancement. Google and Microsoft read-side adapters propagate the cancellation signal to fetch. Added deterministic guard/cancellation/no-persistence/signal-policy coverage and bumped the core package to 0.46.0.

Reviewable only after the exact final source/documentation head passes `Validate production core`. Live provider cancellation, Supabase heartbeat/RLS/RPC execution, durable cursor progression, and multi-worker scheduler behavior remain infrastructure-blocked.
