# 2026-08-10 — P7AG retry worker lease-token fencing

- Continued from exact P7AF head `2027be197b5590cc9b0325af362204da45931f41`.
- Audited retry-worker crash/reclaim behavior and found two production gaps: final mutations were filtered only by worker id, and expired rows left in `leased` state were not eligible for reclaim.
- Added a rotating per-claim retry lease token and a service-role-only fenced finalization RPC that row-locks and verifies exact job/worker/token/unexpired ownership before success, reschedule, or dead-letter mutation.
- Expired leased jobs are now reclaimable with `FOR UPDATE SKIP LOCKED`; stale workers cannot create dead letters or mutate retry state after lease replacement.
- Dead-letter insertion and terminal retry-job mutation now occur in one database transaction. Failure attempts must advance exactly once and reschedule timestamps remain bounded to 1 second–15 minutes.
- Preserved P7AF canonical safe diagnostics and added deterministic lease-loss, malformed-claim, backend-sanitization, atomic-finalization, and migration-contract coverage.
- Bumped package version to `0.50.0`.
- GitHub Actions exact-head validation remains required before P7AG is reported reviewable. Live Supabase migration/RLS, true multi-worker contention, crash/restart, and deployed retry execution remain infrastructure-blocked.
