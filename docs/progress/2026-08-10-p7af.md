# 2026-08-10 — P7AF retry worker diagnostic persistence safety

- Audited the P7AE retry path and found the legacy retry worker still copied raw executor exception text into durable retry/dead-letter diagnostics.
- Reused the canonical sync-failure safety boundary for retry reschedule and dead-letter persistence.
- Unknown stored reasons now fail closed to `sync_failure`; retry/dead-letter `last_error` values are stable application-owned messages only.
- Added deterministic coverage for reschedule/dead-letter behavior and leakage of bearer-like tokens, URLs, email addresses, and arbitrary provider text.
- Bumped package version to `0.49.0` and updated `BUILD_ROADMAP.md`.
- Live deployed worker, Supabase RLS/persistence, and owner-visible dead-letter validation remain blocked by unavailable infrastructure in this build context.
