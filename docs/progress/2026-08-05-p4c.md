# 2026-08-05 — P4C Memory and Semantic Search

- Added explicit user-owned memory lifecycle with edit, expiry, deletion, revision, and audit records.
- Added tenant-isolated semantic document contracts, cosine ranking, and cross-tenant rejection.
- Added a server-side OpenAI embeddings adapter using the current `/v1/embeddings` endpoint.
- Added a user-bound Supabase semantic retrieval adapter.
- Added pgvector storage, HNSW indexing, owner-read RLS, and a service-role-only match function.
- Added retrieval audit persistence and source provenance.
- Added deterministic tests for memory ownership, expiry/deletion, ranking isolation, OpenAI request shape, Supabase ownership, and retrieval auditing.
- Live OpenAI and Supabase pgvector validation remain blocked by missing configured infrastructure.
