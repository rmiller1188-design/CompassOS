# 2026-08-08 — P7O telemetry-safe worker diagnostics

- Continued from reviewable P7N provider-session credential containment.
- Added a dedicated application telemetry sanitizer that recognizes contained provider sessions without reading access-token getters or invoking token capabilities.
- Added redaction for secret-bearing keys, bearer credentials, JWT-like values, and OAuth query parameters.
- Added deterministic SHA-256 pseudonymous account references so routine worker telemetry does not emit raw connected-account IDs.
- Added bounded recursion/string handling, circular-reference safety, conservative Error serialization, and a worker telemetry event envelope.
- Added deterministic leakage and malformed-input tests and included the module in production validation.
- Draft PR opened against P7N. Reviewable status remains gated on GitHub Actions for the exact final documentation head.
- Live worker/APM behavior remains infrastructure-blocked; no deployed telemetry claim is made.
