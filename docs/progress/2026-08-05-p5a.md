# 2026-08-05 — P5A Reply Drafting and Approval Integrity

- Added a server-side OpenAI Responses API reply-drafting adapter with strict JSON Schema output and `store: false`.
- Added source-message provenance validation to reject invented citations.
- Added canonical outbound reply payloads with normalized recipients and deterministic SHA-256 hashes.
- Added reviewable field-level payload diffs.
- Added approval-integrity verification that requires reapproval after any payload change.
- Added deterministic tests for request shape, provenance, normalization, hashing, diffs, incomplete payloads, and post-approval mutation rejection.
- Preserved read-only provider scopes; no send adapter or provider execution path was introduced.
- Live OpenAI quality evaluation and Supabase encrypted outbound-payload persistence remain blocked by missing configured infrastructure.
