# 2026-08-08 — P7Q single-use provider credential capability

- Started from exact P7P head `61657bdaba4931e7a6e4a05b0d7a205ad4343804`.
- Added a consumable one-shot `withAccessToken` capability with a 120-second default TTL.
- The capability is consumed before provider callback execution, so concurrent and replay attempts fail closed.
- Expired capabilities never invoke provider code; callback errors and credential-escape attempts cannot restore or reuse the credential capability.
- Telemetry trust now requires `capabilityUseMode: single-use` and continues to exclude raw provider credentials and connected-account IDs.
- GitHub Actions `Validate production core` run 410 passed the implementation candidate under Node 22.23.1 with production syntax checks and 230/230 deterministic tests.
- Live Google/Microsoft adapters, provider OAuth execution, Supabase service-role integration, and deployed worker/APM behavior remain unvalidated.
